import React from 'react'

const Navbar = () => {
    return (
        <nav className="nav">
            <div>DISCUSSION FORUM</div>
            <button>LOGIN</button>
        </nav>
    )
}

export default Navbar
