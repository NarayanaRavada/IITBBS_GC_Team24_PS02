import React from 'react'

const Navbar = () => {
    return (
        <nav className="nav">
            <div>DISCUSSION PORTAL</div>
            <button>LOGIN</button>
        </nav>
    )
}

export default Navbar
